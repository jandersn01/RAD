services:
  web:
      build: .
      command: python manage.py runserver 0.0.0.0:8000
      volumes:
        - .:/app #bind Mount
                 #Poderia docker volume reais para guardar dados do container
      ports:
        - "8000:8000" #host:container